group 'yapily'
version '0.1.1-SNAPSHOT'
artifactId 'yapily-sdk-examples'

buildscript {
    repositories {
        mavenCentral()
        maven {
            url "http://maven.yapily.com/"
        }
        mavenLocal()
    }
}

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
    maven {
        url "http://maven.yapily.com/"
    }
}

dependencies {
    compile group: 'yapily', name: 'sdk', version: '0.1.2-SNAPSHOT'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
